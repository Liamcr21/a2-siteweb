{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
 <main class="container mt-3">
        <div class="row g-5">
            <div class="col">
                <h1 class="pb-4 mb-4 fst-italic border-bottom">{{ category.name }}</h1>
                <div id="articles-list">
                    {% include 'article/list-categorie.html.twig' with { articles: category.articles, leftCol: 4, rightCol: 8 } %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
